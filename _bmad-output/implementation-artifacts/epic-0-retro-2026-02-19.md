# Epic 0 Retrospective (2026-02-19)

## Epic Summary

- Epic: `0` (Platform Kernel Hardening)
- Story status: `10/10 done`
- Retrospective status: complete
- Closure decision: approved for Epic 1 kickoff

## What Went Well

- Epic 0 scope was delivered completely despite review churn and late adjustments.
- Kernel hardening outcomes were validated across tenancy, auth/session, CSRF, envelope, events/outbox, and readiness controls.
- Team adapted quickly to review findings and maintained delivery momentum.

## What Hurt

- Regression instability outside focused story scope created uncertainty during closure.
- Story artifact/file-list drift required repeated reconciliation.

## Root-Cause Findings

- Primary instability cause was environment execution inconsistency (loopback resolution/connectivity), not confirmed kernel behavior regressions.
- Several failed runs were executed without both backend/frontend live and reachable.

## Stabilization Evidence

- Platform API `@P0` subset: `24 passed`
- Non-API `@P0` subset: `13 passed, 5 skipped`
- Full regression with locked env (`API_URL=http://127.0.0.1:3000`, `BASE_URL=http://127.0.0.1:5173`): `100 passed, 13 skipped, 0 failed`

## Approved Action Items

1. Add mandatory pre-close regression checkpoint to each story record (targeted + full-suite snapshot).
   - Owner: Senior Dev
2. Add artifact sync checklist gate before merge (file list, changelog, sprint-status key, review notes).
   - Owner: Product Owner + Scrum Master
3. Maintain unstable-suite watchlist with owner + signature + current status.
   - Owner: QA Engineer

## Policy Decision (Locked)

Playwright regression execution is now policy-enforced:

- Run against `127.0.0.1` loopback only.
- Verify backend/frontend reachability before tests start.
- Enforced by `scripts/run-playwright-with-preflight.sh`.
- Enforced via npm scripts:
  - `test:e2e`
  - `test:e2e:headed`
  - `test:e2e:debug`

## Next Epic Readiness

- Epic 1 dependencies on Epic 0 kernel controls are satisfied.
- No open Epic 0 blocker remains after stabilization verification.
- Recommendation: proceed with Epic 1 kickoff.
